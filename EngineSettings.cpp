#include "EngineSettings.h"

// -- Global instance -- 
// This is where the memory is actually allocated in your engine's binary (i.e., ensures it is compiled into exactly one translation unit).
EngineSettings g_EngineSettings;
